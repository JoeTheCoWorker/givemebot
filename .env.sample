# Giveaway Bot Configuration
# Towns Protocol bot for managing cryptocurrency giveaways

# Your Towns app private data (base64 encoded)
APP_PRIVATE_DATA=your_app_private_data_here

# JWT secret for webhook authentication
JWT_SECRET=your_jwt_secret_here

# Port to run the bot on (optional, defaults to 3000)
PORT=3000
